const add = (x, y) => x + y;
add(2, 4); // 6


const subtract = (x, y) => x - y;
add(4, 2); // 2


const divide = (x, y) => x / y;
add(4, 2); // 2

const multiply = (x, y) => x * y;
add(4, 2); // 8

// значение возвращается на основании заданных параметров, независимо от того, где/когда вы его вызываете.
// Еще можно переформулировать как
// 1) Не зависит от других
// 2) Не влияет на других

